# Getting Started

Create a project, work through strategy and Why reasoning, estimate with PERT, create tasks, design communications, record evidence, use the analysis labs, and generate the final report.

Main routes include `/strategy`, `/wizard`, `/five-whys`, `/pert`, `/tasks`, `/communications`, `/clay-tablets`, `/journal`, `/market`, `/finance`, `/risk`, `/learn`, `/handbook`, `/reports`, and `/admin/users`.
